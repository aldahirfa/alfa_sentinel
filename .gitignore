# Entornos virtuales de Python (agent/.venv, server/.venv)
.venv/
venv/
__pycache__/
*.pyc

# Secretos -- NUNCA subir esto a GitHub
server/.env
agent/agent_credential.json

# Archivos de prueba locales (no son parte del código de la tesis)
agent/test_files/

# Editor / SO
.vscode/
.idea/
Thumbs.db
.DS_Store

# Certificados TLS (claves privadas de la CA y del servidor) -- NUNCA subir
server/certs/
agent/certs/

# Estado del firewall guardado durante un aislamiento (se restaura al liberar)
agent/isolation_state.json

# Registro del agente cuando corre como servicio
agent/logs/
